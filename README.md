Set up Python Virtual Environment: python3 -m venv venv Activate Python Virtual Environment: source venv/bin/activate Install requirements: pip install -r requirments.txt Install playwright: playwright install Use Pytest: pytest Start application: docker-compose up -d

github: github.com/Trapmaster4113/m12 dockerhub: hub.docker.com/r/tlc23/mod12